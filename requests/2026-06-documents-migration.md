# Documentation migration

The current documentation is written in `.md` files. However the expectation is that all documentation is written in `.docx` files.
Can you make a copy of each markdown file in docx format. Don't delete the old markdown files, just let them live side by side.
Currently the only documents that can be transformed are everything in `/Team` and `/Individual/Soufian Manai, 1114385/*`
Ensure with the transformation that any embedded images are also embedded at the same location in the docx file.
Images live alongside the markdown files, and are standard embedded [Text](img.png) like pathing
